services:
  tautulli:
    image: ghcr.io/tautulli/tautulli:latest
    container_name: tautulli
    restart: unless-stopped
    volumes:
      - ./config:/config
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-Europe/Paris}
    extra_hosts:
      # Permet d'atteindre Plex (network_mode: host) via host.docker.internal:32400
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: curl -fsS http://localhost:8181/status || exit 1
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s
    networks:
      - proxy_manager

networks:
  proxy_manager:
    external: true
