# Fichier de configuration de l'updater
# Une entrée par ligne = un fichier docker-compose.yml à surveiller
# Les lignes commençant par # sont ignorées
# Utiliser des chemins ABSOLUS

# Stack principale (arr-stack, duplicati, NPM, ntfy, plex, pyload, tautulli, torrenting, homepage)
/home/homelab/homelab/docker-compose.yml
